# 범위를 넓혀 온 과정과 진행 기록

[← README](../README.md)

## 질문이 바뀔 때마다 넓힌 범위

처음부터 이 모습을 설계한 것은 아니다. 뉴스와 LLM 해석은 처음 계획에도 있었지만 구현은 기술적
지표와 재무 채점부터 했고, 장중 기록(1분봉, 장전 관찰 목록, 실시간 차트)은 계획에 없었다. 실제
데이터를 돌릴 때마다 앞의 답이 다음 질문을 만들었고, 그 질문에 답하려면 새 데이터나 새 영역이
필요했다. 아래 번호는 질문의 흐름이고 커밋 순서와 꼭 같지는 않다. 자세한 내용은 각 절에 있다.

**1. 과거 성과를 믿을 수 있나.** 백테스트가 조용히 미래를 보는 문제는 처음부터 주된 문제로 두었다.
모든 데이터에 "시장이 언제 알 수 있었나"와 "우리가 언제 확보했나"를 따로 기록하고, 정정은 덮지 않고
쌓고, 판단은 다음 세션 시가에만 체결되게 했다. 그런데도 삼성전자 10년 수익률(v0.2 규칙)은 맞기
전에 세 번 틀렸다(+608% → +475% → +460%, 맞는 값은 +502%). 네 숫자 중 무엇도 실패하거나 경고하지
않았고, 틀린 셋은 맞는 값만큼 그럴듯해 보였다. 원인은 수집 범위, 커버리지 계산, 그리고 DART가
2018년에 바꾼 회계 네임스페이스(`ifrs` → `ifrs-full`)였다. 그 뒤로 수집기는 요청이 성공했는지가 아니라 연도별로 개념을 몇 개 인식했는지를
세고, 공시보다 긴 기간의 런은 거부한다.
→ [시점 정합 설계](design/point-in-time.md#시점-정합-설계), [삼성 수치는 맞기 전에 세 번 틀렸다](studies/backtest.md#삼성-수치는-맞기-전에-세-번-틀렸다)

**2. 무엇을 볼까.** 손으로 고른 18종목은 채점할 수는 있어도 새 종목을 찾아 주지는 않는다. KOSPI·
KOSDAQ 2,648개 상장사의 이름 마스터를 만들고, 네이버 뉴스를 모아 평소보다 갑자기 많이 언급되는
종목을 찾는다. DART 사건 공시와 네이버 검색 추세는 뒤에 더했다. 무료 API 한도를 넘지 않도록 모든 호출은 보내기 전에 원장에
예약하고, 공식 한도의 절반을 예산으로 둔다.
→ [후보 발굴과 승격](design/news-and-llm.md#후보-발굴과-승격-4-2), [DART 사건 공시](design/news-and-llm.md#dart-사건-공시-4-5-개정),
[검색 관심도](design/news-and-llm.md#검색-관심도-4-5-개정-datalab)

**3. 그 기사가 정말 그 회사 이야기인가.** 1종목 → 10종목 → 전체로 넓혀 가며 돌렸다. 10종목에서
원림(정원이라는 뜻의 단어이기도 하다)의 확정 언급 18건 중 7건이 정원 기사였고, 카페24는 검색어가
`카페`와 `24`로 쪼개져 첫 100건 중 96건이 무관했다. 그래서 판정을 층으로 나눴다. 규칙은 확실한
것만 확정·거부하고, 애매한 것은 보류로 남겨 LLM에게 묻는다. 카페24는 판정이 아니라 검색 문제라
질의를 바꿨다(확정 4건 → 15건). 전체 롤아웃에서 확정 건을 유형별로 읽어 보니 3음절 한글 사명은 약
73%만 그 회사 이야기여서, 짧은 이름일수록 더 많은 문맥을 요구하게 했다. 판정은 덮어쓰지 않고
이력으로 쌓는다.
→ [LLM 판정과 기사 해석](design/news-and-llm.md#llm-판정과-기사-해석-롤아웃-8단계-4-3)

**4. 뉴스가 판단에 제때 들어가나.** 처음 채점했을 때 새로 승격한 종목의 뉴스 점수가 0이었다.
KSS해운의 자사주 기사는 17:10에야 수집·해석됐고, 신호는 15:30 마감 시점의 판단이라 그 뒤의 기사를
보지 않는다. 규칙은 옳게 동작했고, 틀린 것은 작업 순서였다. 해석 작업을 마감 전(09:30)으로, 관찰
목록이 생긴 뒤에는 장 전으로 옮겼다. 뉴스는 이보다 앞서, 기본 점수에 섞지 않고 반감기를 가진
이벤트 오버레이로 점수 옆에 붙이기로 정해 두었다.
→ [이벤트 오버레이](design/news-and-llm.md#이벤트-오버레이-4-4)

**5. 고른 종목이 그날 실제로 어떻게 움직였나.** 일봉 점수는 하루에 한 번, 장이 끝난 뒤에 나온다.
아침에 고른 종목이 그날 몇 시에 움직였는지는 말해 주지 못한다. 그래서 한국투자증권 API로 1분봉을
받기 시작했다. 분봉 시각이 그 분의 시작인지, 하루 봉이 최대 몇 개인지, 체결이 없는 분은 어떻게
오는지는 문서 대신 실제 호출 12회로 정했다. 이 앱키는 0.1초 간격의 두 번째 호출에서 속도 초과로
거절됐고 초당 1회에서도 가끔 거절돼서, 지금은 2초에 1회로 부른다.
→ [한국투자증권](design/intraday.md#한국투자증권-phase-5), [1분봉 정본](design/intraday.md#1분봉-정본-5-3),
[장중 분석과 누적 보고서](design/intraday.md#장중-분석과-누적-보고서-5-5-5-6)

**6. 장중에도 보고 싶다.** 실시간 체결을 웹소켓으로 받아 차트로 보여 준다. 화면은 보는 용도이고
기록이 아니다. 분석의 정본은 장 마감 뒤 받는 1분봉이라, 연결이 끊기거나 화면을 켜지 않은 날에도
기록에는 구멍이 나지 않는다.
→ [실시간 차트](design/intraday.md#실시간-차트-5-4)

**7. 매일 새로 고르기.** 첫 관찰 목록 규칙(V1)은 추적 종목을 추적 중이라는 이유만으로 넣었다. 조용한
종목이 매일 자리를 차지해 단타 관찰에 맞지 않아서, 그날 사건이 있는 종목만 고르는 규칙(V2)으로
바꿨다. 9/21~9/23 아침으로 다시 계산해 보니 세 날 모두 0개였는데, 기준값 탓이 아니라 그 아침들에는
아직 시장 전체 뉴스와 공시를 수집하지 않았기 때문이었다. 9/25 데이터로 계산하면 공시가 몰려 40개
상한이 찼고, 공시만 있는 종목끼리는 내부 종목 id가 순서를 정하고 있었다. 그 순서는 공시의 강도로 정하게 고쳤다.
→ [단타용 아침 흐름](design/morning-list.md#단타용-아침-흐름-preopen_v2)

## 진행 상황

**Phase 1 완료** — 자격증명 없이도 세로 한 줄이 끝까지 돈다. 관심종목, yfinance에서 받은
일봉과 환율, 기술적 팩터 엔진, 세 시각이 모두 강제된 신호 조립, 그리고 모든 지표에 대해
raw → 정규화 → 가중치 → 기여도를 drawer에서 보여주는 대시보드.

**Phase 2 완료.**

| | |
| --- | --- |
| SEC EDGAR 시점 복원 | 완료 |
| 공시 등록부 (우리가 태깅한 것이 아니라 공표된 것을 증명) | 완료 |
| 하나의 회계기간에 고정된 재무 엔진 | 완료 |
| 기본층 점수: 기술적 0.6 + 재무 0.4 | 완료 |
| DART 수집기 | 완료 |
| 한국 재무 | 완료 |

**Phase 3 완료** — 시점 저장소, 체결 시계, 이벤트 기반 엔진, 비용, 지표, 홀드아웃을 한 번만
가져가는 walk-forward, 저장된 행으로부터의 완전한 재현, CLI와 Backtest 화면.

| | |
| --- | --- |
| `available_at` + `ingested_at`을 한 문에서 강제 | 완료 |
| `decision_at` → `execution_at` 불변식, `SAME_CLOSE`는 표현 불가 | 완료 |
| IN/OUT_OF_SAMPLE과 런당 홀드아웃 1개를 갖는 walk-forward | 완료 |
| 런 신원: 전략 + 커밋 + 데이터 스냅샷 | 완료 |
| 저장된 12개 측정값을 전부 대조하는 재현 | 완료 |
| 재무 커버리지: 시작점, 중간 구멍, 오른쪽 끝, anchor 개념 | 완료 |
| CLI(`backtest run / show / holdout / reproduce`)와 Backtest 화면 | 완료 |

**Phase 3.5 완료** — 18종목으로 확장. 목적은 종목 수가 아니라 검증이었고, 첫날에 두 개의
종목이었다면 보이지 않았을 결함 셋을 꺼냈다: Postgres 65535 파라미터 한도, IFRS 지배주주/총액
혼용, `--period max`가 Apple 1980년 상장에서 터뜨린 달력 범위 초과. 밸류 성향 가설은
15종목에서 반증됐다(위).

마지막 항목이던 횡단면 정규화가 들어갔다. `percentile_rank`는 Phase 1부터 작성만 돼 있고
부르는 곳이 없었는데, 그동안 `normalize`의 docstring은 횡단면 percentile이 기본이라고
단언하고 있었다 — 이 저장소가 가장 싫어하는 종류의 결함이 자기 설명 안에 있었던 셈이다.
지금은 재무의 단조 비율 넷이 실제로 순위로 채점되고, 그 주장이 주장이 아니라 검사다.

순위를 감당 가능하게 만든 것은 캐시다. 점수가 동종군에 의존하면 세션마다 시장 전체의 재무를
다시 만들어야 하고, 삼성 2024-01 이후 665세션 × 9종목이면 5,985회다. 시점 스냅샷은 어떤 팩트가
사용 가능해지는 순간에만 바뀌므로 그 순간을 기준으로 캐시하면 정확히 같은 답이 나온다.
실측으로 **5,985회가 34회**가 됐고, 런 시간은 26.4초에서 32.8초로 늘었다.

**시점 유니버스 재구성은 의도적으로 하지 않는다.** 설계는 상장폐지 종목을 포함한 과거
종목 마스터를 요구했다. 지수를 살아남은 모습이 아니라 당시 모습대로 돌릴 수 있도록. 한국은
그런 마스터를 무료로 공개하는 곳이 없고, 그 작업이 사주는 것은 **결과가 유니버스 전반에
일반화된다고 주장할 권리**뿐이다. 이것은 지목된 종목들에 대해 보고하는 개인 분석 도구이므로
그 주장을 하지 않는다. 그리고 그렇게 말하는 것은 조용히 빠뜨리는 것과 다르다. 따라서 여기서
생존편향은 완전하며, 교정하지 않고 명시한다.

**횡단면 순위가 그 편향의 도달 범위를 넓혔다.** 동종군은 지금 상장돼 있는 종목들이지 해당
구간에 상장돼 있던 종목들이 아니다. 2018년 세션의 순위가 2026년의 워치리스트를 상대로
매겨진다는 뜻이고, 고치려면 여기서 만들지 않겠다고 적은 바로 그 종목 마스터가 필요하다.
그래서 순위는 "그때 시장 대비"가 아니라 "지금 이 목록 대비, 그때 알려진 수치로"다.

**Phase 4 진행 중** — 첫 증분(4-1)은 끝났다. 뉴스를 모을 자리와, 무료 API 한도를 절대 넘지
않는 장치다. 그 위에서 상장 마스터를 적재하고 뉴스를 세 단계로 넓혀 갔다.

| | |
| --- | --- |
| 호출 원장 (분 단위 버킷, 롤링 윈도) | 완료 |
| 쿼터 그룹 단위 예산, 공식 한도의 50% | 완료 |
| 뉴스·언급 스키마, 시점 규칙 | 완료 |
| 네이버 뉴스 수집기 | 완료 |
| 한국 상장 마스터 (DART `corp_cls`로 KOSPI/KOSDAQ 구분) | 완료 |
| 하루 2회 스케줄 (개장 1시간 전, 마감 후) | 완료 |
| 종목별 거부율 보고 (단계적 롤아웃의 판단 근거) | 완료 |
| 외부 응답 경계: 타입, 폭, NUL·서로게이트, 오버플로 | 완료 |
| 마스터 실적재 (2,648종목: KOSPI 831, KOSDAQ 1,817) | 완료 |
| 뉴스 1 → 10종목 롤아웃 | 완료 — 오탐과 검색어 문제를 찾음 |
| 검색 결과별 판정 기록, 관련성 규칙 | 완료 |
| 뉴스 전체 롤아웃 (2,648종목, 호출 2,648회) | 완료 — 짧은 사명의 정밀도 문제를 찾음 |
| 판정 이력 append-only (`news_relevance_decision`) | 완료 |
| 3음절·짧은 영문 사명의 문맥 규칙 (롤아웃 7단계, 규칙 5) | 완료 |
| 종목별 스윕 커버리지 기록 | 완료 |
| 보류(PENDING) 판정의 LLM 심사 (롤아웃 8단계) | 완료 — 처리는 구독 한도 안에서 나눠 진행 |

Phase 4 전체는 이 번호로 부른다.

| 단계 | 내용 | 상태 |
|---|---|---|
| 4-1 | 이름 마스터, 네이버 뉴스 원자료 수집, 쿼터 원장, 롤아웃 | 완료 |
| 4-2 | 뉴스 freshness: `WallClockFreshnessRule`을 실제 경로에 | 완료 — 발굴과 오버레이에 연결, 영향력의 기간은 4-4의 감쇠 |
| 4-3 | LLM 감성·이벤트 해석 (`news_sentiment`), LLM 사용량 원장 | 완료 — 해석은 추적 종목부터 나눠 진행 |
| 4-4 | 이벤트 오버레이: 기본 점수는 그대로, 감쇠, 같은 사건 기사 묶기 | 완료 — 반감기는 사전 가정, 4-8이 교정 |
| 4-5 | 개정: DART 사건 공시 → 오버레이, 검색 관심도(DataLab). Threads·Reddit은 승인 불가로 보류 | 완료 — 관심도는 신호 옆에 기록만 |
| 4-6 | 후보 발굴 → 승격 | 완료 (4-3보다 먼저 만들었다) |
| 4-7 | 시장 국면 (추세, 변동성, 폭) | 완료 — 신호 옆에 기록만, 행동은 그대로. 포워드 리포트가 국면별로 나눈다 |
| 4-8 | 포워드 테스트 | 기록 중 — 매일 루프, 신호·후보 성과. 검토 관문과 결정 규칙은 미리 고정(`review`). 판단은 2026-12 말 이후 |

Phase 5는 장중 관찰과 기록이다. 아침에 볼 종목을 고정하고, 장중에는 차트로 보고, 장 마감 후
1분봉으로 실제 움직임을 기록해 쌓는다.

| 단계 | 내용 | 상태 |
|---|---|---|
| 5-0 | 한국투자증권 응답의 의미 확인 (실제 호출 12회) | 완료 — 아래 "한국투자증권 (Phase 5)" |
| 5-1 | 인증(토큰·접속키 재사용)과 한도 | 완료 |
| 5-3 | 1분봉 정본 저장과 60세션 백필 | 완료 — 백필은 매일 저녁 나눠서 채운다 |
| 5-2 | 장전 관찰 목록 고정, LLM 해석 08:30 | 완료 — 첫 목록은 9/28 08:50 |
| 5-5 | 장중 분석(MFE·MAE·시간대·초과수익) | 완료 |
| 5-6 | 누적 보고서 | 완료 — 질문 일곱 개를 미리 고정(H6·H7은 첫 목록 전에 추가), 판단은 목록 60일 뒤 |
| 5-4 | 실시간 차트 | 완료 — 실제 체결 연결 확인은 9/28 개장 |
| V2 | 단타용 아침 흐름(`PREOPEN_V2`): 07:00 체인, 후보 풀, 사전 수집, 보충, 관찰용 점수 | 완료 — 첫 실전은 9/28 07:00. V1 목록은 만들지 않는다 |

**검색 결과는 그 회사 이야기라는 증거가 아니다.** 10종목 롤아웃에서 바로 드러났다. 원림은
상장사이면서 정원을 뜻하는 단어라, 확정된 언급 18건 중 7건이 정원 기사였다. 한글로만 된 두 글자
사명은 193개이고 남성, 노을, 나노 같은 이름이 들어 있다. 그래서 층을 나눴다.

| 테이블 | 담는 것 |
|---|---|
| `news_item` | 발행된 기사. 사실 |
| `news_query_hit` | 어느 종목 검색이 그 기사를 가져왔나. 사실, 한 번 쓰고 건드리지 않는다 |
| `news_relevance_decision` | 그 검색 결과에 대한 판정 하나하나. **덧붙이기만 한다** |
| `news_mention` | 최신 판정이 CONFIRMED인 것만. "지금 우리가 보는 것" |

**판정은 덮어쓰지 않는다.** 처음에는 판정을 검색 결과 행에 담고 제자리에서 갱신했다. 그러면
09/23에 CONFIRMED였다가 09/24 새 규칙으로 PENDING이 된 기사는 PENDING만 남고, mention도
지워져서 "그날은 언급으로 취급했다"를 되살릴 수 없었다. 판정이 같으면 시각은 두고 스니펫만
바꿔서, 판정 시각과 판정이 읽은 글의 시점이 어긋나기도 했다. 재무 `semantic_version`에서
겪은 시점 누수와 같은 구조다. 지금은 판정이 바뀌거나 읽은 글이 바뀔 때마다 새 행이 붙고,
시각은 데이터베이스가 찍는다. 과거 시점을 묻는 코드(포워드 테스트, 재현, 후보 발굴)는
`news_mention`이 아니라 `news_repo.decisions_asof`를 읽는다.

판정은 셋이다. 이름이 없으면 REJECTED. 이름이 있고 일반 단어와 헷갈릴 수 없으면 CONFIRMED.
두 글자 순한글 사명처럼 헷갈릴 수 있으면 문맥을 본다 — 제목이 `원림,`이나 `원림은`으로
시작하거나, 종목코드나 `(주)`가 붙으면 강한 신호 하나로 충분하고, 실적·수주·공시 같은 약한
신호는 둘이 필요하다. 그래도 모르면 PENDING으로 **버리지 않고 남긴다.** 나중에 모델이 판정할
대상이다. 판정마다 이유가 `decision_reason`에 남는다.

신호는 **단어의 경계**를 지켜야 한다. 첫 규칙은 이름 뒤 공백을 지우고 조사를 봐서
`원림 이야기`를 `원림이`로, `남성 가수`를 `남성가`로 읽고 확정했다. 지금은 조사가 이름에 바로
붙고 거기서 단어가 끝나야 한다. `예상보다` 속 `상보`처럼 다른 단어 안에서만 걸린 이름은 이름이
아니므로 REJECTED(`absent:inside_word`)이고, `㈜남성산업`은 남성의 `㈜` 신호가 아니다.

거부된 결과까지 남기는 이유는 거부율을 나중에 다시 계산하고, 규칙이 바뀌면 옛 판정을
재판정하기 위해서다. `python -m app.cli rejudge-news`가 API 호출 없이 그 일을 한다. 이때
읽는 것은 기사의 요약이 아니라 **그 판정이 읽은 스니펫**(`news_query_hit.snippet`)이다.
네이버는 검색어 주변을 잘라 스니펫을 주므로 같은 기사라도 종목마다 글이 다르고,
`news_item.summary`에는 처음 가져온 검색의 것만 남는다. 스니펫이 없는 옛 판정은 무엇을 읽었는지
모르므로 재판정하지 않고 건너뛴다(다음 수집이 창 안의 기사를 다시 판정한다). 규칙이 모델의
판정을 덮어쓰지 않고, 판정 시각은 판정이 바뀔 때만 움직인다.

**검색어가 쪼개지는 사명도 있다.** 네이버는 `카페24`를 `카페`와 `24`로 나눠 검색해서 첫
100건 중 96건이 무관했다. 이건 판정이 아니라 검색 문제라서 질의를 바꾼다(`카페24 쇼핑몰`,
확정 4건 → 15건). 검색 문법(따옴표 정확 일치)은 공식 API 계약이 아니므로 쓰지 않는다. 남성이나
나노처럼 너무 흔한 단어도 같은 부류다 — 결과 100건이 전부 일반 기사라 회사 기사는 첫 페이지에
들지도 못한다. 전체 롤아웃의 "보류가 많은 종목" 목록이 다음 질의 교체 후보다.

**짧은 이름일수록 더 많은 문맥을 요구한다.** 전체 스윕(규칙 3)의 확정 건을 유형별로
30건씩 읽었다. 3음절 한글 사명은 경계를 지키게 한 뒤에도 약 73%만 그 회사였고(나무가 무성,
LoL의 제우스, 오로라 패턴), 3자 이하 영문 약어는 약 80%였다(CS 대행, KD 사업, DSR). 그래서
문맥 필수 대상을 3음절 이하 순한글과 3자 이하 영문으로 넓혔고, 필요한 약한 신호 수는 2자
이름 2개, 3자 이름 1개로 두었다. 확정으로 남은 건은 표본에서 거의 전부 맞았다. 대가는
재현율이다. 3음절 확정은 약 4,600건에서 약 1,900건으로 줄고 나머지는 보류로 간다. 보류는
버린 것이 아니라 LLM이 심사할 대상이다.
